# 🚀 X-AI-Reply 快速指南

## 📦 最新改进 (v2.0)

### ✨ 新的语气选择器设计

#### 使用流程
```
1. 点击回复框右上角的 ⚡ AI 按钮
2. 在弹出的语气选择器中：
   - 直接点击语气按钮（💼 专业、😊 友善等）
   - 需要添加标签？点击右上角 🏷️ 按钮
   - 需要切换模型？点击底部状态栏的 ⚙️ 图标
3. 选择语气后自动生成回复
4. 从回复列表中选择一条插入
```

#### 界面布局
```
┌─────────────────────────────────┐
│ 选择回复语气          [🏷️] [✕]  │  ← 标题栏
├─────────────────────────────────┤
│ 📌 标签模式 (点击 🏷️ 展开)      │  ← 可折叠区域
│   ○ 无标签                       │
│   ● River 嘴撸                   │
├─────────────────────────────────┤
│ [💼 专业]  [😊 友善]  [😄 幽默] │  ← 主操作区
│ [🤝 支持]  [🤔 提问]            │
├─────────────────────────────────┤
│ 💡 当前使用: DeepSeek  [⚙️]     │  ← 状态栏（点击展开）
└─────────────────────────────────┘
```

---

## 🎯 核心功能

### 1. AI 回复生成
- **5 种预设语气**: 专业、友善、幽默、支持、提问
- **自定义语气**: 在配置页面添加
- **多模型支持**: DeepSeek、硅基流动、OpenRouter、阿里云百炼

### 2. 标签模式
- **快速添加标签**: 一键插入预设标签（如 @RiverdotInc #River4fun）
- **多标签管理**: 支持创建多个标签模式
- **灵活切换**: 随时启用/禁用标签

### 3. 语料库
- **个性化回复**: 添加你的常用表达和专业术语
- **风格学习**: AI 会参考语料库生成更符合你风格的回复

---

## ⚙️ 配置指南

### 首次使用
1. 打开扩展配置页面
2. 在"AI 配置"标签中：
   - 选择 AI 提供商
   - 输入 API Key
   - 选择模型
3. 保存配置
4. 前往 Twitter 开始使用！

### 添加自定义语气
1. 打开"语气管理"标签
2. 点击"添加语气"
3. 填写：
   - 语气名称（如：技术极客）
   - 语气提示词（描述这种语气的特点）
4. 保存

### 创建标签模式
1. 打开"标签模式"标签
2. 点击"添加标签模式"
3. 填写：
   - 模式名称（如：River 活动）
   - 标签内容（如：@RiverdotInc #River4fun）
4. 保存

---

## 🎨 设计理念

### 为什么移除了 AI 检测功能？
- **聚焦核心**: 产品的核心价值是"帮助用户生成高质量回复"
- **避免矛盾**: 用户使用 AI 生成回复，却要检测别人是否用 AI，逻辑上矛盾
- **降低复杂度**: 减少不必要的功能，让产品更易用

### 为什么重新设计语气选择器？
- **主次分明**: 语气选择是主要任务，模型切换是次要任务
- **流程连贯**: 标签选择内联展开，不跳转页面
- **渐进展示**: 按需展开，不一次性展示所有信息

---

## 🔧 技术栈

- **框架**: React + TypeScript
- **构建工具**: Vite + Turborepo
- **样式**: Tailwind CSS (配置页面) + Inline Styles (内容脚本)
- **存储**: Chrome Storage API
- **国际化**: 自定义 i18n 包

---

## 📝 开发指南

### 项目结构
```
X-AI-reply/
├── pages/
│   ├── options/          # 配置页面
│   ├── popup/            # 弹出窗口
│   └── content-ui/       # Twitter 注入脚本
│       └── src/matches/twitter/
│           ├── App.tsx                    # 主应用
│           ├── components/
│           │   ├── ToneSelector.tsx       # 语气选择器 ✨ 新设计
│           │   ├── ReplyList.tsx          # 回复列表
│           │   └── ConfigPrompt.tsx       # 配置提示
│           ├── hooks/
│           │   ├── useReplyGeneration.ts  # 回复生成逻辑
│           │   └── useAIDetection.ts      # (已移除)
│           └── utils/
│               ├── tweetContent.ts        # 推文内容提取
│               └── notifications.ts       # 通知工具
├── packages/
│   ├── storage/          # 存储管理
│   ├── i18n/             # 国际化
│   └── ui/               # UI 组件库
└── chrome-extension/
    └── src/background/   # 后台脚本
```

### 添加新功能
1. 在 `packages/storage` 中定义数据结构
2. 在 `pages/options` 中添加配置界面
3. 在 `pages/content-ui` 中实现功能逻辑
4. 在 `chrome-extension/src/background` 中处理 API 调用

---

## 🐛 常见问题

### Q: 为什么点击 AI 按钮没反应？
A: 检查是否已配置 API Key。首次使用会弹出配置提示。

### Q: 生成的回复不符合我的风格？
A: 在"语料库"中添加你的常用表达，AI 会学习你的风格。

### Q: 如何切换 AI 模型？
A: 在语气选择器底部点击 ⚙️ 图标，选择其他模型。

### Q: 标签模式在哪里设置？
A: 在语气选择器中点击右上角 🏷️ 按钮，或在配置页面的"标签模式"标签中管理。

---

## 🎉 更新日志

### v2.0 (2025-01-29)
- ✅ 移除 AI 检测功能，聚焦核心价值
- ✅ 重新设计语气选择器，优化交互流程
- ✅ 标签选择内联展开，不再跳转页面
- ✅ 模型选择移至底部状态栏，默认折叠
- ✅ 代码优化，减少 200+ 行冗余代码

### v1.0 (2025-01-XX)
- 🎉 首次发布
- ✨ 支持 5 种预设语气
- ✨ 多 AI 模型支持
- ✨ 标签模式功能
- ✨ 语料库功能

---

## 📞 反馈与支持

- **GitHub Issues**: [提交问题](https://github.com/your-repo/issues)
- **Discord**: [加入社区](https://discord.gg/your-invite)
- **Email**: your-email@example.com

---

**享受更智能的 Twitter 回复体验！** 🚀
